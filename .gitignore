# Do not track the runtime user data file
user_data.pkl

# Allow only the structured backups
!user_backup_*.pkl

# Don't track compiled files or cache
__pycache__/
*.py[cod]

# Don't track temporary logs or local env
*.log
.env
